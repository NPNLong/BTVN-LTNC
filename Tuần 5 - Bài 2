#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    vector<int> v;
    int n; cin >> n;
    for (int i=0; i<n; i++){
        int j;
        cin >> j;
        v.push_back(j);
    }
    
    sort (v.begin(),v.end());
    
    for (int i=0; i<n; i++){
        cout << v[i] << ' ';
    }
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */   
    return 0;
}
